services:
  - type: web
    name: apsp-website
    runtime: python
    pythonVersion: "3.10"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn wsgi:app"
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.14"
      - key: FLASK_ENV
        value: production
